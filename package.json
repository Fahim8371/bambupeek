{
  "name": "bambupeek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1430 --strictPort",
    "build": "tsc --noEmit && vite build",
    "tauri": "tauri",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "check:privacy": "python3 scripts/check_privacy.py"
  },
  "dependencies": { "@tauri-apps/api": "^2.11.0" },
  "devDependencies": { "@tauri-apps/cli": "^2.11.0", "typescript": "~6.0.3", "vite": "^8.3.0" }
}
